"""Stage 4 do pipeline DVC: avaliação do modelo treinado.

PLACEHOLDER — dono: Etapa 4 (dataset + modelo).
"""


def main() -> None:
    """TODO(etapa4): calcular métricas (>= 4) e logar no MLflow — ver params.yaml/evaluate."""
    raise NotImplementedError("Implementar avaliação do modelo — ver params.yaml/evaluate.")


if __name__ == "__main__":
    main()
