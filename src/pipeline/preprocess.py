"""Stage 1 do pipeline DVC: preprocessamento dos dados brutos.

PLACEHOLDER — dono: Etapa 4 (dataset + modelo).
Este arquivo só existe para o `dvc.yaml` ter um comando válido para rodar.
A lógica real de leitura/limpeza do dataset escolhido deve ser implementada
por quem ficar responsável pelo dataset e pelo modelo (Etapa 4).
"""


def main() -> None:
    """TODO(etapa4): ler `params.yaml -> preprocess` e limpar o dataset real."""
    raise NotImplementedError(
        "Implementar preprocessamento do dataset real (ex.: Instacart, "
        "RetailRocket ou MovieLens) — ver params.yaml/preprocess."
    )


if __name__ == "__main__":
    main()
