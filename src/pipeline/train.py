"""Stage 3 do pipeline DVC: treino do modelo com PyTorch + tracking no MLflow.

PLACEHOLDER — dono: Etapa 4 (dataset + modelo).
"""


def main() -> None:
    """TODO(etapa4): treinar o modelo, comparar com baseline sklearn e logar no MLflow."""
    raise NotImplementedError(
        "Implementar treino (PyTorch) com tracking MLflow — ver params.yaml/train."
    )


if __name__ == "__main__":
    main()
